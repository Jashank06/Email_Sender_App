# Production Environment Configuration
# Copy this file to .env and fill in your actual values

# Server Configuration
PORT=3000
NODE_ENV=production

# OTP Email Configuration (Optional - only if you need auth endpoints)
# These are used for sending OTP emails for authentication
OTP_GMAIL_USER=your-otp-email@gmail.com
OTP_GMAIL_PASSWORD=your-gmail-app-password

# Google Service Account
# Mount serviceAccount.json as volume (see docker-compose.yml)
# DO NOT put credentials directly in .env file

# Security Notes:
# 1. Never commit .env file to git
# 2. Use secrets management in production (Docker secrets, Kubernetes secrets, etc.)
# 3. Rotate credentials regularly
# 4. Use read-only volumes for sensitive files
